// === is equality operator
// !== not equal operator
// < less than operator
// > greater than operator
// >= greater than or equal to
// <= less than or equal to

let temp = 100


if (temp <= 32) {
  console.log('It is freezing outside!')
}

if  (temp >= 100) {
  console.log ('It is too hot outside!')
}

// Under 7 is a child discount
// over 65 discount
// Age to my age
// Calculate is child
//calculage senior
//print to screen

let age = 3

if (age <= 7) {
  console.log('Child')
}

if (age >= 65) {
  console.log('Senior')
}